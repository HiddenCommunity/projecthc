extends ../layout

block content
    h1.
         #{title}
    ul
        - each member, i in members
            li
                = member.nickname
                = member.join_date
                = member.majors

                form(action='/members/#{member._id}/edit',method='post',enctype='application/x-www-form-urlencoded')
                    input(type='hidden',value='DELETE',name='_method')
                    button(type='submit').
                        Delete
                p 
                    a(href='/blobs/#{member._id}/edit') Edit
                p
                    a(href='/blobs/#{member._id}') Show